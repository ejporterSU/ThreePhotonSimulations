\documentclass[tikz, border=2pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta, decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}

% =======================================================
% Color definitions
% =======================================================
\definecolor{c689}{HTML}{FF0000}   % Omega_1 transition (689 nm)
\definecolor{c688}{HTML}{E33434}   % Omega_2 transition (688 nm)
\definecolor{c679}{HTML}{780202}   % Omega_3 transition (679 nm)

% =======================================================
% Layout parameters
% =======================================================

% Level line half-widths
\def\lw{0.55}

% Zeeman sublevel vertical splitting
\def\dz{0.3}

% Horizontal offset between m_j sublevels
\def\offset{2}

% Detuning offset (dashed virtual levels)
\def\detune{0.3}

% Label x-offset (state term symbols on the left)
\def\lboff{-3.2}

% m_j header vertical offset above top level
\def\mjoff{0.5}

% --- X centers (all manifolds share x = 0) ---
\def\xGnd{0}
\def\xPo{0}    % 3P0
\def\xPz{0}    % 3P1
\def\xSo{0}    % 3S1

% --- Y positions (increasing energy upward) ---
\def\yGnd{-0.5}  % 1S0  ground state
\def\yPo{1.2}    % 3P0  (~14504 cm^{-1})
\def\yPz{2.5}    % 3P1  (~14836 cm^{-1})
\def\ySo{5.0}    % 3S1  (~29038 cm^{-1})

% --- Common styles ---
\tikzset{
  arrow/.style = {ultra thick, ->, >=stealth},
}
\def\bit{0.15}


% % version 2
% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
%   (\xGnd-\offset - \lw - \bit, \yGnd - \bit) rectangle (\xGnd-\offset + \lw + \bit, \ySo + \detune + \bit);

% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
% (\xGnd - \lw - \bit , \yGnd - \bit) rectangle (\xGnd + \lw + \bit, \ySo + \detune + \bit);
% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
%   (\xGnd+\offset - \lw - \bit , \yGnd - \bit) rectangle (\xGnd+\offset + \lw + \bit, \ySo + \detune + \bit);


% version 3
% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
%   (\xGnd+\lboff - 0.5, \yGnd - 0.5) rectangle ++(\xGnd+\offset + \lw + 2*0.5 - \lboff, 1);

% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
%   (\xGnd+\lboff - 0.5, \yPo - 0.5 ) rectangle ++(\xGnd+\offset + \lw + 2*0.5 - \lboff, 1);

% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
%   (\xGnd+\lboff - 0.5, \yPz - \detune - 0.3 ) rectangle ++(\xGnd+\offset + \lw + 2*0.5 - \lboff, 0.9 + 2*\detune);
% \draw[rounded corners=1pt, blue!10, line width=1.2pt,
%       fill=blue!5, draw=blue!10]
  % (\xGnd+\lboff - 0.5, \ySo - \detune/2 - 0.3 ) rectangle ++(\xGnd+\offset + \lw + 2*0.5 - \lboff, 0.6 + \detune);






% =======================================================
% Energy levels
% =======================================================

% --- 1S0 (ground state, no sublevels) ---
\draw[thick] (\xGnd-\lw, \yGnd) -- (\xGnd+\lw, \yGnd);


% --- 3P0 (clock state, no sublevels) ---
\draw[thick]  (\xPo-\lw, \yPo)          -- (\xPo+\lw, \yPo);
\draw[thick, dashed] (\xPo-\lw, \yPo-\detune) -- (\xPo+\lw, \yPo-\detune);
\draw[<->, >=stealth, font=\small]
  (\xPo+\lw, \yPo-\detune) -- (\xPo+\lw, \yPo)
  node[midway, right] {$\Delta_3$};


% --- 3P1 (three Zeeman sublevels: m_j = +1, 0, -1) ---
\draw[thick] (\xPz-\lw-\offset, \yPz+\dz) -- (\xPz+\lw-\offset, \yPz+\dz);  % m_j = +1
\draw[thick] (\xPz-\lw,         \yPz)      -- (\xPz+\lw,         \yPz);       % m_j =  0
\draw[thick] (\xPz-\lw+\offset, \yPz-\dz)  -- (\xPz+\lw+\offset, \yPz-\dz);  % m_j = -1
% detuning marker on m_j = +delta
\draw[thick, densely dashed]
  (\xPz-\lw-\offset, \yPz+\dz+\detune) -- (\xPz+\lw-\offset, \yPz+\dz+\detune);
\draw[<->, >=stealth, font=\small]
  (\xPz- \offset+\lw + 0.05, \yPz) -- (\xPz- \offset+\lw + 0.05, \yPz+\dz+\detune)
  node[midway, right] {$\Delta_1$};

% zeeman
\draw[dashed]
(\xPz-\lw-\offset, \yPz) -- (\xPz+\lw+\offset, \yPz);
\draw[<->, >=stealth, font=\small]
(\xPz-\lw+\offset, \yPz) -- (\xPz-\lw+\offset , \yPz-\dz)
node[midway, left] {$\delta_B$};


% --- 3S1 (three Zeeman sublevels: m_j = +1, 0, -1) ---
\draw[thick] (\xSo-\lw+\offset, \ySo-\dz/2) -- (\xSo+\lw+\offset, \ySo-\dz/2);  % m_j = +1
\draw[thick] (\xSo-\lw,         \ySo)        -- (\xSo+\lw,         \ySo);          % m_j =  0
\draw[thick] (\xSo-\lw-\offset, \ySo+\dz/2)  -- (\xSo+\lw-\offset, \ySo+\dz/2);   % m_j = -1
% detuning marker on m_j = 0
\draw[thick, dashed]
  (\xSo-\lw, \ySo+\detune) -- (\xSo+\lw, \ySo+\detune);
\draw[<->, >=stealth, font=\small]
  (\xSo+\lw , \ySo) -- (\xSo+\lw, \ySo+\detune)
  node[midway, right] {$\Delta_2$};


% =======================================================
% Transition arrows (three-photon pathway)
% =======================================================

% Omega_1: 1S0 -> 3P1 (m_j = +1, detuned)
\draw[arrow, c689, font=\small]
  (\xGnd, \yGnd) -- (\xPz-\offset, \yPz+\dz+\detune)
  node[midway, left] {$\Omega_1, \mathbf{k}_1$};

% Omega_2: 3P1 (m_j = +1) -> 3S1 (m_j = 0, detuned)
\draw[arrow, c688, font=\small]
  (\xPz-\offset, \yPz+\dz+\detune) -- (\xSo, \ySo+\detune)
  node[midway, above left] {$\Omega_2,\mathbf{k}_2$};

% Omega_3: 3S1 (m_j = 0) -> 3P0 (detuned)
\draw[arrow, c679, font=\small]
  (\xSo, \ySo+\detune) -- (\xPo, \yPo-\detune)
  node[midway,  right] {$\Omega_3, \mathbf{k}_3$};




% =======================================================
% boxes
% =======================================================


%% version1
% \draw[rounded corners=3pt, gray!30, line width=1.2pt,
%       fill=gray!5, draw=gray!40]
%   (\xGnd+\lboff - 0.3, \yGnd - 0.3) rectangle (\xSo+\lboff + 0.3, \ySo + 0.3);

% \draw[rounded corners=3pt, gray!30, line width=1.2pt,
%     fill=gray!5, draw=gray!40]
% (-\lw - \offset, \ySo+\mjoff) rectangle ++(2*\offset + 2*\lw, 0.8);

% % version3
\draw[gray!5, line width=0pt,
      fill=gray!15, draw=gray!15]
  (\xGnd+\lboff - 0.3, \yGnd - 0.3) rectangle (\xSo+\lboff + 0.3, \ySo + \mjoff);

\draw[blue!5, line width=0pt,
    fill=gray!15, draw=gray!15]
(\xGnd+\lboff - 0.3, \ySo+\mjoff) rectangle (\lw + \offset, \ySo+\mjoff + 0.8);


% % version4
% \draw[gray!80, densely dashed] (\xGnd-\offset-\lw - 0.5, \ySo+\mjoff) -- (\lw + \offset, \ySo+\mjoff);




  
\node[font=\small] at (\xGnd+\lboff, \yGnd) {${}^1\!S_0$};
\node[font=\small] at (\xPo+\lboff, \yPo) {${}^3\!P_0$};
\node[font=\small] at (\xPz+\lboff, \yPz) {${}^3\!P_1$};
\node[font=\small] at (\xSo+\lboff, \ySo) {${}^3\!S_1$};


% =======================================================
% m_j column headers
% =======================================================



\node[font=\small, above] at (\xGnd-\offset, \ySo+\mjoff)       {$+1$};
\node[font=\small, above] at (\xGnd,         \ySo+\mjoff)       {$0$};
\node[font=\small, above] at (\xGnd+\offset, \ySo+\mjoff)       {$-1$};
\node[font=\small, above] at (\xGnd,         \ySo+\mjoff+0.3)   {$m_j$};





\end{tikzpicture}
\end{document}